
import React from 'react';

// This component is no longer in use as the AI image generation feature has been removed.
// It is kept to prevent breaking imports if it were referenced somewhere, but it renders nothing.
export default function CustomizeBirdModal() {
  return null;
}
